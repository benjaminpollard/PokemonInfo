<resources>

    <style name="PokemonCard" parent="Widget.MaterialComponents.CardView">
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">@dimen/card_padding</item>
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="contentPadding">@dimen/card_padding</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardBackgroundColor">@color/pokedex_screen_background_colour</item>

    </style>

    <style name="pokemonEntry" parent="Widget.MaterialComponents.TextView">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/pixel</item>

    </style>

</resources>
